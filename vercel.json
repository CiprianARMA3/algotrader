{
  "buildCommand": "cd my-app && npm install && npm run build",
  "outputDirectory": "my-app/.next",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    }
  ]
}